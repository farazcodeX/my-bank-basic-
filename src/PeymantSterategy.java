public interface PeymantSterategy {
    // in interface we just define a method 
    // they are not abstract mehod i think
    public void pay(double amount);

    public String getPaymentDetale();
}